---
description: Позвать ПЯТНИЦУ — наставника по проектам (статус / ревью / план / затык / объясни / идея)
argument-hint: "[режим] [проект или вопрос]"
---

Вызови субагента `friday` через инструмент Agent (`subagent_type: "friday"`,
`run_in_background: false`) и передай ему запрос босса: $ARGUMENTS

Если аргументов нет — попроси у него режим «статус» по всему репозиторию.

Отчёт субагента босс не видит, поэтому перескажи его ответ целиком,
сохранив структуру и тон ПЯТНИЦЫ, ничего не сокращая.
